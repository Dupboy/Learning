#include <stdio.h>
#include <stdbool.h>

int main(){

	//Logical operators && (and) checks if 2 conditions are true
	
	float temp = 25;

	if (temp >= 0}{
		printf("The weather is good today");
	}
	else{
	  printf("The weather is bad");


	}













return 0;
}
